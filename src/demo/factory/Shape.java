package demo.factory;

public interface Shape {
    /**
     * The function "draw" is used to draw something, but the details of what is being drawn are not
     * specified.
     */
    void draw();
}
